# Frontend API base URL
# Development uses Vite proxy, so /api avoids CORS issues.
# For deployed frontend without a reverse proxy, set the full backend URL, e.g. https://api.example.com/api
VITE_API_URL=/api

# Backend target used by the local Vite proxy.
VITE_PROXY_TARGET=http://localhost:5001
